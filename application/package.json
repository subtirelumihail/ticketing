{
  "name": "ticketing",
  "productName": "OpenDev Ticketing System",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "NODE_ENV=development electron ./app.js",
    "build-dev": "NODE_ENV=production electron ./app.js",
    "clean": "rm -rf build",
    "build": "npm run clean && electron-packager ./ --platform=darwin --arch=x64 --out ./build --overwrite"
  },
  "author": "Subtirelu Mihail",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^7.0.2",
    "electron-prebuilt": "^1.1.1"
  }
}
